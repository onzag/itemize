/**
 * Contains the dev dependencies that the parent project
 * should have, these are for building the project with esbuild
 * @module
 */

export default {
  "esbuild": "0.19.8",
  "esbuild-sass-plugin": "^2.16.0",

  // puppeteer for testing
  "@types/puppeteer": "^3.0.2",
  "puppeteer": "^5.3.1",
};
