/**
 * This file generates some basic source so that the prokect works
 * out of the box
 */
import { ISetupConfigType } from "..";
export default function srcSetup(arg: ISetupConfigType): Promise<ISetupConfigType>;
