// the service worker must somehow be invalidated
// TODO
// const CACHE_NAME = "V1";

// self.addEventListener("install", (event: any) => {
//   event.waitUntil(
//     caches.open("v1").then((cache) => {
//       return cache.addAll([
//         "/",
//         "/rest/resource"
//       ])
//     });
//   );
// });
